#include <bits/stdc++.h>
using namespace std;

using ll = long long;

void solve() {
    int k, x;
    cin >> k >> x;

    cout << (k*x)+1 << endl;

    //if(x%2==0) cout << (k*x)+1 << endl;
    //else if(x%2!=0) cout << (k+x) << endl;
    
    
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);

    int t;
    cin >> t;
    while (t--) {
        solve();
    }

    // solve();

    return 0;
}