#include <bits/stdc++.h>
using namespace std;

void solve() {
    int M, N;
    if (!(cin >> M >> N)) return;

    cout << (M * N) / 2 << "\n";
}

int main() {
    ios_base::sync_with_stdio(false);
    cin.tie(NULL);
    
    solve();
    
    return 0;
}